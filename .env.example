# Server Configuration
PORT=4000
NODE_ENV=development
CLIENT_URL=http://localhost:3002

# Database (PostgreSQL via Docker on port 5433)
DATABASE_URL=postgresql://postgres:postgrespassword@localhost:5433/safety_gate

# In-Memory Cache & Session Store (Redis via Docker on port 6380)
REDIS_URL=redis://localhost:6380
